    map $http_host $frontend {
        hostnames;
        default /frontend/mhoper;
        static.hoper.xyz /static;
        m.hoper.xyz /frontend/mhoper;
        .hoper.xyz /frontend/hoper;
        .liov.xyz /frontend/mhoper;
        localhost /frontend/mhoper;
    }

    map $http_host $static {
        hostnames;
        default /static;
        static.hoper.xyz /static;
        .hoper.xyz /static/hoper;
        .liov.xyz /static/liov;
        localhost /static/hoper;
    }