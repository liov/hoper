apiVersion: v1
kind: Service
metadata:
  name: hoper
spec:
  selector:
    app: hoper
  portLevelSettings:
    - name: http
      port: 80
      targetPort: 8090
    - name: grpc
      port: 8090
      targetPort: 8090