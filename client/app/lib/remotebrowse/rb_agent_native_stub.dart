class RbAgentNative {
  static Future<void> run(String signalWs, String room, {String? sandbox, int iceTimeoutMs = 15000}) async {
    throw UnsupportedError('远程相册 Agent 仅支持 iOS / Android / macOS / Windows / Linux');
  }
}
